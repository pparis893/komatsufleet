_schema-version: "3.2"
ID: komatsufleet
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: komatsufleet-destination-content
  type: com.sap.application.content
  requires:
  - name: komatsufleet-destination-service
    parameters:
      content-target: true
  - name: komatsufleet-repo-host
    parameters:
      service-key:
        name: komatsufleet-repo-host-key
  - name: komatsufleet-auth
    parameters:
      service-key:
        name: komatsufleet-auth-key
  parameters:
    content:
      subaccount:
        destinations:
        - Name: komatsufleet_html_repo_host
          ServiceInstanceName: komatsufleet-html5-service
          ServiceKeyName: komatsufleet-repo-host-key
          sap.cloud.service: komatsufleet
        - Authentication: OAuth2UserTokenExchange
          Name: komatsufleet_auth
          ServiceInstanceName: komatsufleet-xsuaa-service
          ServiceKeyName: komatsufleet-auth-key
          sap.cloud.service: komatsufleet
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: komatsufleet-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: komatsufleet-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - komatsufleet.zip
      name: komatsufleet
      target-path: resources/
- name: komatsufleet
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: komatsufleet-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: komatsufleet-destination-service
    service-plan: lite
- name: komatsufleet-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
      tenant-mode: dedicated
      xsappname: komatsufleet-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-name: komatsufleet-xsuaa-service
    service-plan: application
- name: komatsufleet-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: komatsufleet-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
